/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */

package Ultilitarios;

/**
 *
 * @author Nicksson
 */
public class FuncaoJTB {
    /*
    public static Contas pegarContaSelecionada(int linhaSelecionada){
            Contas c = new Contas();
            c.setIdConta(ContasDAO.listaContas.get(linhaSelecionada).getIdConta());
            c.setIdContaPai(ContasDAO.listaContas.get(linhaSelecionada).getIdContaPai());
            c.setReferencia(ContasDAO.listaContas.get(linhaSelecionada).getReferencia());
            c.setDataCompra(ContasDAO.listaContas.get(linhaSelecionada).getDataCompra());
            c.setDataVencimento(ContasDAO.listaContas.get(linhaSelecionada).getDataVencimento());
            c.setValorConta(ContasDAO.listaContas.get(linhaSelecionada).getValorConta());
            c.setValorRestante(ContasDAO.listaContas.get(linhaSelecionada).getValorRestante());
            c.setTipoConta(ContasDAO.listaContas.get(linhaSelecionada).getTipoConta());
            c.setIdEspecificacao(ContasDAO.listaContas.get(linhaSelecionada).getIdEspecificacao());       
            return c;
    }
      public static Especificacao pegarEspecificacaoSelecionada(int linhaSelecionada){
            Especificacao e = new Especificacao();
            e.setIdEspecificacao(EspecificacaoDAO.listaEspecificacao.get(linhaSelecionada).getIdEspecificacao());
            e.setNmEspecificacao(EspecificacaoDAO.listaEspecificacao.get(linhaSelecionada).getNmEspecificacao());
            return e;
    }
    */
}
